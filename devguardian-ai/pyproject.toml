[project]
name = "devguardian-ai"
version = "0.1.0"
description = "AI-powered autonomous CI failure diagnosis and repair system."
authors = [
    { name = "Arundhoti Chatterjee" }
]
readme = "README.md"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"